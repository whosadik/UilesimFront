## Backend notes (ProfilePage)

### 1) `/api/me/favorite-category` — не хватает полей под текущий UI

Сейчас ответ даёт `favorite_category`, `window_days`, `profile_complete`, `explain`, но UI хочет ещё:

* `products_bought` (кол-во покупок/позиций)
* `total_spent` (сумма расходов за окно)

В текущем контракте этих полей нет. 

**Что сделать на бэке:**

* Расширить response `GET /api/me/favorite-category` (или сделать отдельный endpoint) и добавить:

  * `products_bought: int` (определить точную метрику: “кол-во покупок” = кол-во транзакций? или сумма qty по item’ам?),
  * `total_spent: decimal|string` (сумма `total_amount` по транзакциям в окне),
  * опционально `currency` (если в системе не одна валюта).
* Сохранить `explain` как есть (UI показывает “Как мы считаем?”).

### 2) Budget: фронт хочет min/max, бэк хранит enum

Сейчас профиль ожидает `budget` как enum (`low|medium|high`), а ProfileWizard на фронте оперирует `budgetMin/budgetMax`. 

**Варианты на бэке (выбрать один):**

* **Оставить как есть (рекомендую для MVP):** `budget` остаётся enum, а фронт маппит min/max -> enum.
* **Добавить расширение:** принять/отдавать `budget_range: {min, max}` дополнительно к `budget` (или вместо него), но это уже миграции/валидация/совместимость.

### 3) Оффер на странице: в UI сейчас статический

В API уже есть поддержка “следующего оффера” и клика по нему:

* `GET /api/me/next-offer` (возвращает assignment + offer + target + expires_at) 
* `POST /api/offers/click` 

**Что проверить/доделать на бэке (если будут баги в UI):**

* чтобы `expires_at` был всегда (или явно `null`), иначе фронт будет гадать формат;
* чтобы `offer.type/value` были стабильны (value часто decimal-string — это ок, но важно не менять тип поля). 

### 4) Рекомендации “Специально для вас”: формат ответа секциями

`GET /api/me/recommendations/home` возвращает `sections[]` (не плоский массив). 
Фронт сейчас уже пытается поддерживать оба варианта, но **лучше на бэке гарантировать один формат**.

**Что закрепить на бэке:**

* всегда возвращать `{ ok: true, sections: [...] }` (или хотя бы всегда `sections`), чтобы фронт не содержал лишние ветки.

### 5) Транзакции/покупки: если решишь считать агрегаты на бэке

Есть `GET /api/transactions/` (без глобальной пагинации). 
Если транзакций станет много — расчёт “любимой категории” и “total_spent” должен быть агрегатом на SQL уровне, а не “выгрузить всё и посчитать”.

**Итог по обязательному для этой страницы:**

* **обязательное**: расширить `/api/me/favorite-category` полями `products_bought` и `total_spent` (иначе текущий UI не заполнить данными из API). 
* остальное — опциональные улучшения/стабилизация контракта.



Backend notes (для этого компонента)

Avatar

Добавить в бэке поле avatar_url (или avatar) в любом “user/me” endpoint, который фронт и так получает при авторизации (например /api/me), либо расширить профиль.

Display name

Если нужно “Привет, Адик”, а не username: добавить display_name или first_name/last_name в тот же endpoint.

(Необязательно) completionPercentage на бэке

Можно отдавать profile_completion_percent (если хочешь единый расчёт), но сейчас фронтового расчёта достаточно.

Backend notes (только заметки, без правок)

 Для “полного” UI брендов нужен отдельный endpoint, например /api/brands с полями slug/id, name, logo, products_count.
 Текущий подход считает бренды из /api/products/; если появится пагинация/ограничение выдачи, счётчики брендов могут быть неполными.
 Где видно в бэкенде:
catalog/views.py (фильтры, включая brand),
catalog/serializers.py (поле brand в контракте).

Backend notes (только заметки, без правок)

 Какие поля/эндпойнты нужны, чтобы UI стал “полным”: для блока любимой категории нужны агрегаты total_spent, products_bought в GET /api/me/favorite-category.
 Какие несостыковки контракта мешают: UI-лейблы wizard локализованы, а skin_type в API строго enum; для goals/avoid_flags нет строгого словаря в контракте, поэтому нужен явный backend-документированный taxonomy.
 Где это видно: users_app/views.py, users_app/models.py, users_app/serializers.py, loyalty/views.py, recs_app/views.py, offers/views.py, MOCKS_REGISTRY.md.

 Не сделано / Заблокировано

 Что не получилось подключить и почему (нет в API / другой формат / нет поля): у Home нет отдельного API для hero-баннера/мини-статистики; у бренда нет полей описания/рейтинга; у /api/me/next-offer нет массива баннеров (только один активный оффер)
 Какие компоненты/поля оставлены как есть (fallback): Hero целиком статический; в BrandSpotlight описание бренда и рейтинг (4.8) оставлены fallback; в Promotions оставлены дополнительные fallback-карточки помимо первой API-карточки
Backend notes (только заметки, без правок)

 Какие поля/эндпойнты нужны, чтобы UI стал “полным”:
endpoint для hero-контента Home (заголовок/подзаголовок/CTA/баджи/stats),
endpoint/поля бренда (description, rating, logo, slug),
endpoint списка промо-баннеров для Home (не один next-offer, а коллекция)
 Какие несостыковки контракта мешают:
UI ждёт richer product-поля (oldPrice, rating, reviewCount, pointsEarned), а /api/products/ отдаёт базовые каталог-поля;
UI Promotions исторически карточечный список, а /api/me/next-offer отдаёт 1 assignment
 Где это видно (файлы/места в backend-коде или в схемах): catalog/serializers.py, recs_app/views.py, offers/views.py, schema-map.md, get_api_me_recommendations_home__200.json, get_api_me_next_offer__200.json, get_api_products__200.json

Не сделано / Заблокировано

 Что не получилось подключить и почему (нет в API / другой формат / нет поля): notes, opened_at, finish_date не приходят в OwnedProductSerializer, поэтому сохранить их через API нельзя
 Какие компоненты/поля оставлены как есть (fallback): редактирование заметок оставлено локальным (в состоянии страницы), даты opened_at/finish_date остаются пустыми, пока бэкенд их не отдаёт
Backend notes (только заметки, без правок)

 Какие поля/эндпойнты нужны, чтобы UI стал “полным”: поля notes, opened_at, finish_date в контракте owned-products (чтение и patch), либо отдельный endpoint для заметок по owned-продукту
 Какие несостыковки контракта мешают: OpenAPI examples для activate/deactivate показывают полный OwnedProduct, но реальный view возвращает {ok, id, is_active}; также UI ожидает дополнительные поля, которых нет в serializer
 Где это видно (файлы/места в backend-коде или в схемах): backend/transactions/serializers.py (OwnedProductSerializer.fields), backend/transactions/views.py (activate/deactivate response), docs/api-examples/schema-map.md и docs/api-examples/me/post_api_me_owned_products_by_id_activate__200.json / ...deactivate__200.json



 Не сделано / Заблокировано

 Что не получилось подключить и почему (нет в API / другой формат / нет поля): в roadmap API нет UI-полей title/description шага и product_image; поэтому для них оставлены fallback-значения на фронте
 Какие компоненты/поля оставлены как есть (fallback): объясняющий блок points/why/improves/benefit (UI-мета), изображения карточек шага (placeholder), человекочитаемые заголовки шагов (маппинг по product_type)
Backend notes (только заметки, без правок)

 Какие поля/эндпойнты нужны, чтобы UI стал “полным”: в GET /api/me/roadmap полезно добавить для каждого шага title, description, recommended_product.image_url (или расширенный product payload)
 Какие несостыковки контракта мешают: UI использует статусы pending/current/completed, а backend отдаёт missing/recommended/owned/skipped/completed; нужен фронтовый маппинг статусов
 Где это видно (файлы/места в backend-коде или в схемах): serializers.py, models.py, get_api_me_roadmap__200.json, schema-map.md

 Не сделано / Заблокировано

 Что не получилось подключить и почему (нет в API / другой формат / нет поля): в контракте транзакций нет явных полей type, description, status, points_change, tier_after (UI вынужден использовать fallback/эвристику)
 Какие компоненты/поля оставлены как есть (fallback): фильтр по типам работает по вычисленному типу; в деталях позиции показываются как Товар #ID (без имени/изображения); кнопка «Загрузить ещё» оставлена как UI-заглушка (пагинация/мета в текущем контракте не используется)
Backend notes (только заметки, без правок)

 Какие поля/эндпойнты нужны, чтобы UI стал “полным”: для /api/transactions/ и /api/transactions/{id}/ полезно добавить явные type, description, status, points_change, tier_after, а для items — вложенный product summary (name, image_url)
 Какие несостыковки контракта мешают: UI карточка транзакции богаче текущего serializer-контракта; сейчас API отдаёт базу (id, created_at, total_amount, channel, items), поэтому часть полей только вычисляется на фронте
 Где это видно (файлы/места в backend-коде или в схемах): serializers.py, views.py, schema-map.md, get_api_transactions__200.json, get_api_transactions_by_id__200.json


Не сделано / Заблокировано

 Что не получилось подключить и почему (нет в API / другой формат / нет поля): список товаров корзины (id/name/brand/price/quantity/image/pointsEarned) не подключён к API, потому что в backend OpenAPI/schema-map нет /api/me/cart и связанных update/remove endpoints.
 Какие компоненты/поля оставлены как есть (fallback): cartItems и локальные updateQuantity/removeItem; upsell использует fallback product 4, когда next-offer.target.scope не product_id.
Backend notes (только заметки, без правок)

 Какие поля/эндпойнты нужны, чтобы UI стал “полным”: GET /api/me/cart (+ update/remove item) с полями корзины (id, name, brand, price, quantity, image_url, points_earned).
 Какие несостыковки контракта мешают: GET /api/me/next-offer возвращает inline object, и target не гарантирует product_id; для полной upsell-карточки нужен стабильный target.product_id или отдельный cart-upsell endpoint.
 Где это видно (файлы/места в backend-коде или в схемах): schema-map.md, .tmp_backend_openapi.yaml, offers/views.py, checkout_app/views.py, loyalty/views.py, get_api_me_next_offer__200.json, post_api_checkout_preview__200.json, get_api_me_loyalty__200.json.


 Не сделано / Заблокировано

 Что не получилось подключить и почему (нет в API / другой формат / нет поля): нет API-полей для tierUpgraded/newTier, гарантированных discount/pointsUsed/pointsEarned на странице checkout (в GET /api/transactions/ сериализатор отдаёт только id/created_at/total_amount/channel/items), нет step-level roadmap данных (stepNum/title/description/pointsBonus) и карточки продукта (brand/price/image) для блока next step.
 Какие компоненты/поля оставлены как есть (fallback): баннер апгрейда уровня, часть блока "Детали заказа" (скидка, списанные/начисленные баллы при отсутствии полей), roadmap-карточка шага (title/step/bonus), карточка рекомендуемого товара (brand/price/image).
Backend notes (только заметки, без правок)

 Какие поля/эндпойнты нужны, чтобы UI стал “полным”: endpoint для деталей последнего checkout (или расширение GET /api/transactions/), где есть gross_total, discount_amount, points_redeemed, points_earned, new_balance, tier_upgraded/new_tier; и endpoint для "next roadmap step" с шагом и карточкой продукта.
 Какие несостыковки контракта мешают: CheckoutPage ожидает rich checkout snapshot и roadmap-рекомендацию, а текущий transactions контракт минимальный; next-offer даёт только offer/target без product card полей.
 Где это видно (файлы/места в backend-коде или в схемах): schema-map.md, get_api_transactions__200.json, post_api_checkout__201.json, get_api_me_next_offer__200.json, serializers.py, views.py.



 Не сделано / Заблокировано

 Что не получилось подключить и почему (нет в API / другой формат / нет поля): нет backend-контракта для настоящего wishlist (GET/POST/DELETE /api/me/wishlist), поэтому нельзя загрузить именно пользовательское избранное и нельзя сделать реальное удаление из избранного
 Какие компоненты/поля оставлены как есть (fallback): список на странице остаётся fallback-ориентированным (каталог API + локальный fallback), поведение “удалить из избранного” как серверная операция не подключено
Backend notes (только заметки, без правок)

 Какие поля/эндпойнты нужны, чтобы UI стал “полным”: GET /api/me/wishlist/, POST /api/me/wishlist/, DELETE /api/me/wishlist/{product_id}/ с полноценным Product (id/name/brand/price/image/category/in_stock/points_earned)
 Какие несостыковки контракта мешают: для telemetry wishlist-событий текущий /api/me/recommendations/event не подходит под wishlist_view/remove_from_wishlist, т.к. принимает только action=click|add_to_cart
 Где это видно (файлы/места в backend-коде или в схемах): schema-map.md, schema-map.md, users_app/urls.py, recs_analytics/serializers.py

 Не сделано / Заблокировано

 Что не получилось подключить и почему (нет в API / другой формат / нет поля): блок “Next Best Action” не переведён на API, т.к. отдельного endpoint’а next-best-action в текущем контракте нет
 Какие компоненты/поля оставлены как есть (fallback): hero “Next Best Action”, sidebar Roadmap-прогресс (2/5 и шаги), cold-start/demo toggle; также fallback-массивы рекомендаций остаются резервом при пустом/ошибочном ответе API
Backend notes (только заметки, без правок)

 Какие поля/эндпойнты нужны, чтобы UI стал “полным”: endpoint для next-best-action (title/description/benefit/cta/points/roadmap-step) для замены статического hero-блока
 Какие несостыковки контракта мешают:
POST /api/me/recommendations/event принимает только action=click|add_to_cart (нет impression/wishlist/...),
profile.goals хранится как JSON-коды, а UI работает с русскими лейблами (нужен маппинг)
 Где это видно (файлы/места в backend-коде или в схемах):
schema-map.md (/api/me/profile, /api/me/loyalty, /api/me/recommendations/home, /api/me/next-offer, /api/me/recommendations/event),
recs_analytics/serializers.py (choices для action),
users_app/models.py (goals как JSONField),
get_api_me_recommendations_home__200.json (sections/for_you/trending)

Не сделано / Заблокировано

 Что не получилось подключить и почему (нет в API / другой формат / нет поля): счётчики для иконок Избранное и Корзина в Navbar не переведены на API, т.к. нет backend endpoint’ов wishlist/cart для текущего пользователя
 Какие компоненты/поля оставлены как есть (fallback): badge={2} и badge={3} в Navbar.tsx
Backend notes (только заметки, без правок)

 Какие поля/эндпойнты нужны, чтобы UI стал “полным”: GET /api/me/wishlist (минимум count/list) и GET /api/me/cart (минимум count/list) для реальных badge-счётчиков в navbar
 Какие несостыковки контракта мешают: UI-шапка ожидает быстрые счётчики по избранному/корзине, а текущий backend-контракт покрывает auth/profile/loyalty/transactions/checkout, но не хранит отдельные ресурсы wishlist/cart
 Где это видно (файлы/места в backend-коде или в схемах): backend/urls.py, transactions/urls.py, checkout_app/urls.py, FRONTEND_CONTEXT.md
Инференс: отсутствие /api/me/wishlist и /api/me/cart сделано по перечисленным роутам и документации.




Не сделано / Заблокировано

 Что не получилось подключить и почему (нет в API / другой формат / нет поля): полноценное добавление в корзину (нет /api/me/cart), wishlist-операции (нет /api/me/wishlist), а также гарантированные rating/reviews/originalPrice/discount как отдельные поля в контракте PDP
 Какие компоненты/поля оставлены как есть (fallback): handleAddToCart -> navigate('/cart'), fallback-изображение в рекомендациях при отсутствии image в bundle, fallback-тексты для description/ingredients/howToUse, fallback rating/reviews к 0 при отсутствии данных
Backend notes (только заметки, без правок)

 Какие поля/эндпойнты нужны, чтобы UI стал “полным”: GET /api/me/cart (минимум count/items + add/update/remove), GET/POST/DELETE /api/me/wishlist, явные PDP-поля rating, reviews_count, original_price (или стабильный эквивалент)
 Какие несостыковки контракта мешают: catalog отдаёт application_text и ingredients_inci, но не отдельные how_to_use/rating/reviews/original_price; bundle в текущем виде не гарантирует изображения карточек
 Где это видно (файлы/места в backend-коде или в схемах): catalog/serializers.py (список полей), catalog/models.py (контент-поля PDP), recs_app/urls.py (наличие bundle), recs_app/views.py (_load_products без image_url/image_urls), catalog/views.py (GET только для authenticated)

Не сделано / Заблокировано

 Что не получилось подключить и почему (нет в API / другой формат / нет поля): нет отдельного API бренда (описание, логотип, метаданные), нет явного поля/эндпойнта для «хитов» и явного is_new
 Какие компоненты/поля оставлены как есть (fallback): текст описания бренда в hero, логотип как первая буква, вкладка «Хиты» как первые 8 товаров бренда
Backend notes (только заметки, без правок)

 Какие поля/эндпойнты нужны, чтобы UI стал “полным”: GET /api/brands/{slug} (name, description, logo/image, product_count, has_new), плюс источник для «хитов» (is_hit/sales_rank или отдельный endpoint)
 Какие несостыковки контракта мешают: UI бренда ожидает сущность бренда, а backend сейчас отдаёт только товары; «новинки/хиты» не представлены как явный контракт бренда
 Где это видно (файлы/места в backend-коде или в схемах): catalog/views.py (фильтр только по brand__iexact), catalog/serializers.py (поля продукта, но нет сущности бренда), catalog/models.py (модель только Product)


 Не сделано / Заблокировано

 Что не получилось подключить и почему (нет в API / другой формат / нет поля): для ProfileProgressCard нет отдельного API-поля со списком “какие именно поля профиля не заполнены”; для карточек из /api/me/recommendations/home часто нет image_url/image_urls в текущем payload рекомендаций.
 Какие компоненты/поля оставлены как есть (fallback): fallback-список полей в ProfileProgressCard, fallback-изображение/бренд/цена и часть бейджей в ProductCard при неполных данных API, дефолтные тексты в OfferCard для none/expired.
Backend notes (только заметки, без правок)

 Какие поля/эндпойнты нужны, чтобы UI стал “полным”: в ответе /api/me/recommendations/home полезно отдавать image_url/image_urls (и при необходимости original_price/points_earned) в product; для прогресса профиля нужен API-список незаполненных полей.
 Какие несостыковки контракта мешают: Offer в API использует discount|points_multiplier|gift, а UI-слой раньше ждал percentage|points|gift; рекомендации отдают более узкий product-пейлоад, чем карточка товара.
 Где это видно (файлы/места в backend-коде или в схемах): offers/models.py:6, offers/views.py:183, .tmp_backend_openapi.yaml:585, .tmp_backend_openapi.yaml:2027, .tmp_backend_openapi.yaml:2267, recs_app/views.py:134, catalog/serializers.py:5.


Что осталось fallback по причине контракта:

API не отдаёт конфигурацию меню/быстрых ссылок/категорий для mega/mobile меню;
API не отдаёт справочники опций для ProfileWizard (типы/цели/ноты и т.д.);
SearchBar остаётся универсальным компонентом (готов под API, но сам по себе эндпойнт не вызывает).


Не сделано / Заблокировано

 Что не получилось подключить и почему (нет в API / другой формат / нет поля):
для NewArrivalsPage нет отдельного backend-эндпойнта “новинки”/facet-counts, поэтому “новизна” вычисляется на фронте по is_new или created_at;
для PromotionsPage в OpenAPI у /api/me/offers не описана схема ответа, а UI-поля (badge, маркетинговый description, image, cta) напрямую не отдаются
 Какие компоненты/поля оставлены как есть (fallback):
в PromotionsPage сохранён fallback-набор карточек при пустом /api/me/offers + пустом /api/me/next-offer;
в NewArrivalsPage сохранены текущие визуальные фильтры категорий (без серверных facet-counts)
Backend notes (только заметки, без правок)

 Какие поля/эндпойнты нужны, чтобы UI стал “полным”:
отдельный read-only эндпойнт новинок (например, /api/products/new с серверной сортировкой/пагинацией и count), и для акций — UI-ready поля (title, description, badge, image_url, cta_label) в /api/me/offers
 Какие несостыковки контракта мешают:
/api/me/offers в OpenAPI без формальной схемы ответа; offer.type ограничен discount|points_multiplier|gift (нет явного personal), поэтому часть UI-семантики приходится строить эвристикой
 Где это видно (файлы/места в backend-коде или в схемах):
Loyalty Platform API.yaml:384, Loyalty Platform API.yaml:430, Loyalty Platform API.yaml:1043, offers/views.py:183, offers/views.py:333, offers/models.py:6, catalog/serializers.py:8

Не сделано / Заблокировано

 Что не получилось подключить и почему (нет в API / другой формат / нет поля):
для SalePage в контракте продукта нет гарантированных original_price/discount/has_discount полей, поэтому невозможно полностью отказаться от fallback
для RoutinePage в generate нет длительности шага, step-level notes и изображения продукта; в validate альтернативы приходят ID-шниками
для RoadmapPage в recommended_product нет image_url
 Какие компоненты/поля оставлены как есть (fallback):
SalePage: fallback-список скидок при отсутствии discount-данных в API
RoutinePage: duration, часть notes, product_image, fallback-советы
RoadmapPage: product_image placeholder + статические текстовые метаданные шага
Backend notes (только заметки, без правок)

 Какие поля/эндпойнты нужны, чтобы UI стал “полным”:
GET /api/products/ с явными полями скидки (original_price, discount или has_discount) / отдельный фильтр скидок
POST /api/routine/generate с duration, step-level note/instruction, image_url
POST /api/routine/validate с развёрнутыми альтернативами (не только ID)
GET /api/me/roadmap с recommended_product.image_url
 Какие несостыковки контракта мешают:
UI Sale ожидает признаки скидки, а контракт продукта их не гарантирует
UI Routine и Roadmap ожидают richer step/product данные, а backend отдаёт минимальный набор
 Где это видно (файлы/места в backend-коде или в схемах):
Loyalty Platform API.yaml:706, Loyalty Platform API.yaml:1043, Loyalty Platform API.yaml:1184
catalog/serializers.py:8
routines/views.py:42, routines/views.py:108
roadmap_app/serializers.py:45


Не сделано / Заблокировано

 Что не получилось подключить и почему (нет в API / другой формат / нет поля): server-side поиск ?search= не подключён, потому что в текущем backend-контракте нет параметра search для /api/products/.
 Какие компоненты/поля оставлены как есть (fallback): SUGGESTED_QUERIES (популярные запросы) и recentSearches (localStorage) оставлены локальными; FilterBar на странице оставлен визуальным (как было), без server-side фильтрации по query.
Backend notes (только заметки, без правок)

 Какие поля/эндпойнты нужны, чтобы UI стал “полным”: для полноценного API-поиска нужен параметр search (или отдельный search endpoint) для /api/products/.
 Какие несостыковки контракта мешают: UI страницы поиска предполагает текстовый поиск; backend /api/products/ сейчас фильтрует по category/product_type/brand/in_stock, но не по свободному тексту.
 Где это видно (файлы/места в backend-коде или в схемах): views.py, serializers.py, .tmp_backend_openapi.yaml, schema-map.md.

 Не сделано / Заблокировано

 Что не получилось подключить и почему (нет в API / другой формат / нет поля): для AdminMetricsPage в контракте нет временных рядов и channel breakdown в формате графиков; для AdminOverviewPage нет top_offers/top_categories/alerts/recommended_actions в ожидаемом UI-формате; для AdminCampaignDetailPage нет endpoint публикации кампании.
 Какие компоненты/поля оставлены как есть (fallback): часть таблиц/блоков в Overview (offers/categories/alerts/actions), графики Metrics при пустом series/channels, кнопка Publish в CampaignDetail (с явным сообщением, без фейкового успеха), creative banner-заглушка.
Backend notes (только заметки, без правок)

 Какие поля/эндпойнты нужны, чтобы UI стал “полным”: GET /api/admin/metrics с series[] и channels[] для графиков; GET /api/admin/overview с kpis, trend, top_offers, top_categories, alerts, recommended_actions; POST /api/admin/campaigns/{id}/publish; расширение campaign-полей (end date, tiers, promo_text, banner_url).
 Какие несостыковки контракта мешают: Audit фильтруется по user_id, а UI раньше работал как по actor-строке; Cache invalidate принимает запрос, но фактически инвалидирует фиксированный набор recs-ключей; Campaign API отдаёт бюджетную модель, а UI формы ожидают более “маркетинговый” объект.
 Где это видно (файлы/места в backend-коде или в схемах): views_admin.py, views_admin.py, views.py, views.py, views_admin_campaigns.py, schema-map.md, .tmp_backend_openapi.yaml.

 